# numconst 测试说明

## 无独立测试文件

`numconst` 包只包含无类型常量定义（数量级、存储单位、时间单位），没有可执行逻辑，不需要独立测试。

常量的正确性由两处保证：

1. **编译期**：常量值错误会导致使用方编译失败或 overflow 错误
2. **代码审查**：常量值直接可见，无需运行时验证

## 关联验证命令

```bash
# 验证常量定义可正常编译
go build ./pkg/numconst/
```
